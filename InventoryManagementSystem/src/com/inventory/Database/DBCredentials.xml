<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">

<!--
    Este archivo XML contiene los valores para username y password que se requieren para conectar a la
    base de datos MySQL. Por defecto los valores son username=root y password=root.
    Se pueden cambiar en las keys dentro de este archivo xml.
    -->

<properties>
<comment>Credenciales para la base de datos.</comment>
    <entry key="username">root</entry>
    <entry key="password">root</entry>
</properties>
